# i18n Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Install vue-i18n, create zh-CN/en translation dictionaries, and translate all UI text across 20+ components.

**Architecture:** vue-i18n@10 plugin in `src/plugins/i18n.ts`, locale TS files in `src/locales/`, `general.locale` persisted in settings store. Components use `$t('key')` in templates and `useI18n().t('key')` in script.

**Tech Stack:** Vue 3 + TypeScript, vue-i18n@10, Pinia

## Global Constraints

- No existing user data translated — only UI framework text and preset names
- Translation keys use dot notation by module: `topbar.newItem`, `settings.title`, etc.
- Locale stored as `zh-CN` / `en` in localStorage `index-settings.general.locale`
- Default locale: `zh-CN`
- All hardcoded Chinese strings in `.vue` files must be replaced with `$t()` / `t()` calls
- `lang="zh-CN"` is not a valid HTML attribute value — use `lang="zh"` for the html attribute; vue-i18n locale identifier stays `zh-CN`
- SettingsModal "通用" tab gets a language `<select>` dropdown

---

### Task 1: Install vue-i18n, create locale files, plugin, wire main.ts, add general section to settings store

**Files:**
- Create: `src/locales/zh-CN.ts`
- Create: `src/locales/en.ts`
- Create: `src/plugins/i18n.ts`
- Modify: `src/main.ts`
- Modify: `src/stores/settings.ts`
- Bash: `pnpm add vue-i18n@10`

- [ ] **Step 1: Install vue-i18n**

```bash
cd C:/Users/xingy/Desktop/开发/itemmanage && pnpm add vue-i18n@10
```

- [ ] **Step 2: Create Chinese locale file**

Create `src/locales/zh-CN.ts`:

```typescript
export default {
  topbar: { newItem: '新建条目' },
  settings: {
    title: '设置', general: '通用', theme: '主题',
    mode: '模式', light: '浅色', dark: '深色',
    accentColor: '强调色', bgColor: '背景色', textColor: '字体色',
    fontSize: '字号', auto: '自动',
    customTheme: '自定义主题', default: '默认',
    apply: '应用', saveAs: '另存为...', delete: '删除',
    cssOverride: 'CSS 变量覆盖',
    savePreset: '保存为预设', syncFromCSS: '与自定义主题保持一致',
    save: '保存', cancel: '取消',
    language: '语言', moreSoon: '更多设置即将推出',
  },
  centerList: {
    empty: '暂无条目', newItem: '新建条目', deleteItem: '删除条目',
    justNow: '刚刚', minAgo: ' 分钟前', hourAgo: ' 小时前',
    dayAgo: ' 天前', monthAgo: ' 月前', yesterday: '昨天',
  },
  sidebar: {
    groups: '分组', tags: '标签', showAll: '← 显示全部',
  },
  rightPanel: {
    selectItem: '选择条目查看详情',
    detail: '条目详情', properties: '属性',
    groups: '分组', tags: '标签',
    ungrouped: '未分组', noTags: '无标签',
    addGroup: '+ 添加分组', addTag: '+ 添加标签',
    confirm: '确定', cancel: '取消',
    selectGroup: '选择分组...', selectTag: '选择标签...',
  },
  typeManager: {
    title: '类别管理', preset: '预设',
    newType: '新建类别', typeName: '类别名称',
    typeInfo: '类别信息', fields: '字段', items: ' 项',
    addField: '添加字段',
    fieldName: '字段名',
    text: '文本', checkbox: '复选框',
    displayText: '显示文字（可选）',
    noFields: '暂无字段，点击下方添加',
    edit: '编辑', delete: '删除',
    type: '类别', field: '字段',
    confirmDelete: '确定删除{kind} "{name}" 吗？',
    updated: '类别已更新', created: '类别已创建', deleted: '类别已删除',
    fieldAdded: '字段已添加', fieldDeleted: '字段已删除', fieldUpdated: '字段已更新',
    reorderFailed: '排序失败',
    actionFailed: '失败',
  },
  itemDialog: {
    title: '新建条目', name: '条目名称',
    type: '类别', selectType: '选择类别...',
    create: '创建', cancel: '取消',
    created: '条目已创建', failed: '创建失败',
  },
  emptyState: {
    title: 'Index',
    tagline: '本地优先 · 插件扩展 · 对象管理',
    openRepo: '打开仓库', createRepo: '创建新仓库',
    selectFolder: '选择文件夹',
    createFailed: '创建失败',
    openFailed: '无法打开仓库',
  },
  statusBar: {
    items: ' 个条目', path: '路径',
  },
  propertiesForm: {
    noCustomFields: '此类型无自定义属性',
    fieldUpdateFailed: '字段更新失败',
  },
  fileTree: {
    title: '文件',
    newFolder: '新建文件夹',
  },
  iconPicker: {
    search: '搜索图标...', noResults: '无匹配图标',
  },
  groupTree: {
    newGroup: '新建分组', groupName: '分组名称',
  },
  tagList: {
    newTag: '+ 新建标签', tagName: '标签名称',
  },
  toast: {
    createSuccess: '创建成功',
    deleteSuccess: '删除成功',
    updateSuccess: '更新成功',
    renameSuccess: '重命名成功',
    moveSuccess: '移动成功',
    copySuccess: '复制成功',
  },
  common: {
    confirm: '确认', delete: '删除', rename: '重命名',
    openFile: '打开文件',
    close: '关闭',
    settings: '设置', category: '类别管理',
    yes: '确定', no: '取消',
    select: '选择...',
  },
}
```

- [ ] **Step 3: Create English locale file**

Create `src/locales/en.ts`:

```typescript
export default {
  topbar: { newItem: 'New Item' },
  settings: {
    title: 'Settings', general: 'General', theme: 'Appearance',
    mode: 'Mode', light: 'Light', dark: 'Dark',
    accentColor: 'Accent Color', bgColor: 'Background', textColor: 'Text Color',
    fontSize: 'Font Size', auto: 'Auto',
    customTheme: 'Custom Theme', default: 'Default',
    apply: 'Apply', saveAs: 'Save As...', delete: 'Delete',
    cssOverride: 'CSS Variable Override',
    savePreset: 'Save as Preset', syncFromCSS: 'Sync from CSS',
    save: 'Save', cancel: 'Cancel',
    language: 'Language', moreSoon: 'More settings coming soon',
  },
  centerList: {
    empty: 'No items', newItem: 'New Item', deleteItem: 'Delete Item',
    justNow: 'Just now', minAgo: 'm ago', hourAgo: 'h ago',
    dayAgo: 'd ago', monthAgo: 'mo ago', yesterday: 'Yesterday',
  },
  sidebar: {
    groups: 'Groups', tags: 'Tags', showAll: '← Show All',
  },
  rightPanel: {
    selectItem: 'Select an item to view details',
    detail: 'Item Detail', properties: 'Properties',
    groups: 'Groups', tags: 'Tags',
    ungrouped: 'Ungrouped', noTags: 'No tags',
    addGroup: '+ Add Group', addTag: '+ Add Tag',
    confirm: 'Confirm', cancel: 'Cancel',
    selectGroup: 'Select group...', selectTag: 'Select tag...',
  },
  typeManager: {
    title: 'Type Manager', preset: 'Preset',
    newType: 'New Type', typeName: 'Type Name',
    typeInfo: 'Type Info', fields: 'Fields', items: ' items',
    addField: 'Add Field',
    fieldName: 'Field Name',
    text: 'Text', checkbox: 'Checkbox',
    displayText: 'Display text (optional)',
    noFields: 'No fields yet. Add one below.',
    edit: 'Edit', delete: 'Delete',
    type: 'Type', field: 'Field',
    confirmDelete: 'Delete {kind} "{name}"?',
    updated: 'Type updated', created: 'Type created', deleted: 'Type deleted',
    fieldAdded: 'Field added', fieldDeleted: 'Field deleted', fieldUpdated: 'Field updated',
    reorderFailed: 'Reorder failed',
    actionFailed: 'Failed',
  },
  itemDialog: {
    title: 'New Item', name: 'Item Name',
    type: 'Type', selectType: 'Select type...',
    create: 'Create', cancel: 'Cancel',
    created: 'Item created', failed: 'Create failed',
  },
  emptyState: {
    title: 'Index',
    tagline: 'Local-first · Plugin-extensible · Object Manager',
    openRepo: 'Open Repo', createRepo: 'Create Repo',
    selectFolder: 'Select Folder',
    createFailed: 'Create failed',
    openFailed: 'Cannot open repository',
  },
  statusBar: {
    items: ' items', path: 'Path',
  },
  propertiesForm: {
    noCustomFields: 'This type has no custom fields',
    fieldUpdateFailed: 'Field update failed',
  },
  fileTree: {
    title: 'Files',
    newFolder: 'New Folder',
  },
  iconPicker: {
    search: 'Search icons...', noResults: 'No matching icons',
  },
  groupTree: {
    newGroup: 'New Group', groupName: 'Group Name',
  },
  tagList: {
    newTag: '+ New Tag', tagName: 'Tag Name',
  },
  toast: {
    createSuccess: 'Created successfully',
    deleteSuccess: 'Deleted successfully',
    updateSuccess: 'Updated successfully',
    renameSuccess: 'Renamed successfully',
    moveSuccess: 'Moved successfully',
    copySuccess: 'Copied successfully',
  },
  common: {
    confirm: 'Confirm', delete: 'Delete', rename: 'Rename',
    openFile: 'Open File',
    close: 'Close',
    settings: 'Settings', category: 'Type Manager',
    yes: 'Yes', no: 'No',
    select: 'Select...',
  },
}
```

- [ ] **Step 4: Create i18n plugin**

Create `src/plugins/i18n.ts`:

```typescript
import { createI18n } from 'vue-i18n'
import zhCN from '@/locales/zh-CN'
import en from '@/locales/en'

function getInitialLocale(): string {
  try {
    const raw = localStorage.getItem('index-settings')
    if (raw) {
      const parsed = JSON.parse(raw)
      return parsed?.general?.locale ?? 'zh-CN'
    }
  } catch {}
  return 'zh-CN'
}

export const i18n = createI18n({
  legacy: false,
  locale: getInitialLocale(),
  fallbackLocale: 'zh-CN',
  messages: {
    'zh-CN': zhCN,
    en: en,
  },
})
```

- [ ] **Step 5: Wire i18n in main.ts**

In `src/main.ts`, add `import { i18n } from './plugins/i18n'` and chain `.use(i18n)`. The final createApp block:

```typescript
import { createApp } from 'vue'
import { createPinia } from 'pinia'
import { i18n } from './plugins/i18n'
import App from './App.vue'

const app = createApp(App)
app.use(createPinia())
app.use(i18n)
app.mount('#app')
```

- [ ] **Step 6: Add general section to settings store**

In `src/stores/settings.ts`:

Add to `GlobalSettings` interface:
```typescript
interface GeneralSection {
  locale: string
}

interface GlobalSettings {
  theme: ThemeSection
  general: GeneralSection
}
```

Add to `defaultSettings()`:
```typescript
general: { locale: 'zh-CN' },
```

Add to `loadFromStorage()`:
```typescript
general: {
  locale: parsed.general?.locale ?? 'zh-CN',
},
```

Add state refs and expose in the store:
```typescript
const locale = ref('zh-CN')
```

In `load()`:
```typescript
locale.value = settings.general.locale
```

In `save()`:
```typescript
general: { locale: locale.value },
```

Add `setLocale()` action:
```typescript
function setLocale(loc: string): void {
  locale.value = loc
  save()
}
```

Update return statement to include `locale, setLocale`.

- [ ] **Step 7: Verify TypeScript**

```bash
pnpm vue-tsc --noEmit
```
Expected: No errors.

- [ ] **Step 8: Commit**

```bash
git add pnpm-lock.yaml package.json src/locales/ src/plugins/ src/main.ts src/stores/settings.ts
git commit -m "feat: add vue-i18n infrastructure — zh-CN/en locales, plugin, settings integration"
```

---

### Task 2: Add language dropdown to SettingsModal

**Files:**
- Modify: `src/components/SettingsModal.vue`

- [ ] **Step 1: Add language select to General tab**

Replace the placeholder `<p class="placeholder-text">更多设置即将推出</p>` with:

```html
<div class="setting-section">
  <div class="setting-row">
    <label>{{ $t('settings.language') }}</label>
    <select v-model="localLocale" class="lang-select">
      <option value="zh-CN">中文</option>
      <option value="en">English</option>
    </select>
  </div>
</div>
<p class="placeholder-text">{{ $t('settings.moreSoon') }}</p>
```

- [ ] **Step 2: Add localLocale state and wire it**

In `<script setup>`:
```typescript
const localLocale = ref(settingsStore.locale)
```

In `open()` add to snapshot:
```typescript
locale: settingsStore.locale,
```
and init: `localLocale.value = settingsStore.locale`

In snapshot type add: `locale: string`

In `onSave()` add: `settingsStore.setLocale(localLocale.value)`

In `onCancel()` add: `settingsStore.setLocale = ...` — actually, just restore via snapshot:
```typescript
settingsStore.locale = snapshot.locale
// Also update i18n global locale
import { useI18n } from 'vue-i18n'
// ... in setup:
const { locale: i18nLocale } = useI18n()
```

- [ ] **Step 3: Watch localLocale for live preview**

Add to the existing watch (or create a separate one):
```typescript
watch(localLocale, (val) => {
  i18nLocale.value = val
})
```

- [ ] **Step 4: Commit**

```bash
git add src/components/SettingsModal.vue
git commit -m "feat: add language dropdown to SettingsModal general tab"
```

---

### Task 3: Translate shell & dialog components

**Files:**
- Modify: `src/App.vue`, `src/components/Topbar.vue`, `src/components/Sidebar.vue`, `src/components/StatusBar.vue`, `src/components/EmptyState.vue`, `src/components/NewItemDialog.vue`, `src/components/Toast.vue`

- [ ] **Step 1: App.vue** — no hardcoded Chinese text, skip.

- [ ] **Step 2: Topbar.vue**

Replace "新建条目" button text:
```html
<button class="primary" @click="$emit('newItem')">
  <TablerIcon name="plus" :size="16" /> {{ $t('topbar.newItem') }}
</button>
```
Replace `title="设置"` and `title="类别管理"`:
```html
<button class="icon-btn" @click="settingsRef?.open()" :title="$t('common.settings')">
<button class="icon-btn" @click="$emit('openTypeManager')" :title="$t('common.category')">
```

- [ ] **Step 3: Sidebar.vue**

Replace:
```html
<span>分组</span> → <span>{{ $t('sidebar.groups') }}</span>
<span>标签</span> → <span>{{ $t('sidebar.tags') }}</span>
<span v-if="gid !== null" class="clear-hint">← 显示全部</span> → {{ $t('sidebar.showAll') }}
```

- [ ] **Step 4: StatusBar.vue**

```html
{{ repoStore.repoPath || 'Index' }} — {{ repoStore.itemCount }} 个条目
→ {{ repoStore.repoPath || 'Index' }} — {{ repoStore.itemCount }}{{ $t('statusBar.items') }}
```

- [ ] **Step 5: EmptyState.vue**

Translate: title, tagline, "打开仓库" → `$t('emptyState.openRepo')`, "创建新仓库" → `$t('emptyState.createRepo')`, "选择文件夹" → `$t('emptyState.selectFolder')`, etc.

- [ ] **Step 6: NewItemDialog.vue**

Translate: "新建条目" → `$t('itemDialog.title')`, "条目名称" → `$t('itemDialog.name')`, "类别" → `$t('itemDialog.type')`, "选择类别..." → `$t('itemDialog.selectType')`, "创建" → `$t('itemDialog.create')`, "取消" → `$t('itemDialog.cancel')`.

Also in script: `toast.success(...)` messages → use `t()` from i18n.

- [ ] **Step 7: Toast.vue**

Toast component should accept translated strings — no change needed since messages come from callers.

- [ ] **Step 8: Commit**

```bash
git add src/components/Topbar.vue src/components/Sidebar.vue src/components/StatusBar.vue src/components/EmptyState.vue src/components/NewItemDialog.vue
git commit -m "feat: translate shell and dialog components with vue-i18n"
```

---

### Task 4: Translate content components

**Files:**
- Modify: `src/components/CenterList.vue`, `src/components/RightPanel.vue`, `src/components/TypeManager.vue`, `src/components/PropertiesForm.vue`, `src/components/FileTree.vue`, `src/components/FileTreeNode.vue`, `src/components/GroupTree.vue`, `src/components/GroupTreeNode.vue`, `src/components/TagList.vue`, `src/components/ActivityBar.vue`, `src/components/IconPicker.vue`

- [ ] **Step 1: CenterList.vue**

Template: "暂无条目" → `$t('centerList.empty')`, "新建条目" → `$t('centerList.newItem')`, "删除条目" → `$t('centerList.deleteItem')`.

Script: `ago()` function returns hardcoded Chinese time strings → replace with `t('centerList.justNow')`, `t('centerList.minAgo')`, etc. Import `{ useI18n }` → `const { t } = useI18n()`.

- [ ] **Step 2: RightPanel.vue**

ActivityBar tabs: `{ id: 'detail', icon: 'file-description', title: $t('rightPanel.detail') }` — but `tabs` is a const outside setup. Either make it a computed or inline. Best approach: use `$t()` in template where the tab label renders. Actually, tabs are defined as a plain array. Move to `computed`:
```typescript
const tabs = computed(() => [
  { id: 'detail', icon: 'file-description', title: t('rightPanel.detail') },
  { id: 'types', icon: 'category', title: t('common.category') },
])
```

Translate: "属性" → `$t('rightPanel.properties')`, "分组" → `$t('sidebar.groups')`, "标签" → `$t('sidebar.tags')`, "未分组" → `$t('rightPanel.ungrouped')`, etc. "选择条目查看详情" → `$t('rightPanel.selectItem')`.

- [ ] **Step 3: TypeManager.vue**

Heavy translation. All text in template: "类别管理" → `$t('typeManager.title')`, "预设" → `$t('typeManager.preset')`, "新建类别" → `$t('typeManager.newType')`, etc.

Script toast messages: `toast.success(t('typeManager.updated'))` etc.

Delete confirmation: 
```typescript
const kind = deleteTarget.value.kind === 'type' ? t('typeManager.type') : t('typeManager.field')
return t('typeManager.confirmDelete', { kind, name: deleteTarget.value.name })
```

- [ ] **Step 4: PropertiesForm.vue**

"此类型无自定义属性" → `$t('propertiesForm.noCustomFields')`. Field type labels "文本" → `t('typeManager.text')`, "复选框" → `t('typeManager.checkbox')`.

- [ ] **Step 5: FileTree.vue**

"文件" → `$t('fileTree.title')`, "新建文件夹" → `$t('fileTree.newFolder')`.

- [ ] **Step 6: GroupTree.vue**

"+ 新建分组" → `$t('groupTree.newGroup')`, "分组名称" → `$t('groupTree.groupName')`.

- [ ] **Step 7: GroupTreeNode.vue**

Context menu: "重命名" → `$t('common.rename')`, "删除" → `$t('common.delete')`, "创建子分组" → ... add key.

- [ ] **Step 8: TagList.vue**

"+ 新建标签" → `$t('tagList.newTag')`, "标签名称" → `$t('tagList.tagName')`.

- [ ] **Step 9: ActivityBar.vue**

Tab titles translation handled by consumers (RightPanel passes computed titles).

- [ ] **Step 10: IconPicker.vue**

"搜索图标..." → `$t('iconPicker.search')`, "无匹配图标" → `$t('iconPicker.noResults')`.

- [ ] **Step 11: All toast messages in all components**

Script-level toast calls: `toast.success('创建成功')` → `toast.success(t('toast.createSuccess'))` etc.

- [ ] **Step 12: Commit**

```bash
git add src/components/
git commit -m "feat: translate all content components with vue-i18n"
```

---

### Verification Checklist

- [ ] `pnpm vue-tsc --noEmit` — no errors
- [ ] `pnpm dev` — app launches with Chinese text (default)
- [ ] SettingsModal → 通用 → 语言 → switch to English → all UI text switches in real time
- [ ] Theme settings still work in both languages
- [ ] Create new item, type, group, tag → all work with translated UI
- [ ] Light/dark toggle still works
- [ ] `cargo build` — no Rust-side regressions
