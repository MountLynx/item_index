## 修复计划

### 修复 #1：create_item / delete_item 事务保护
- **create_item**：begin tx → INSERT → 建文件夹 → 写 .md → commit。任一步失败回滚并清理。
- **delete_item**：begin tx → DELETE → commit → 删文件夹（best-effort，忽略失败）。

### 修复 #2：update_item 原子化
- 将 name 和 properties 的两次独立 UPDATE 合并为一条动态 SQL（动态构建 SET 子句 + 按序 bind）。

### 修复 #4：list_items 重构为动态 Query Builder
- 将 8 个 if-else 分支（~130 行）改为按需组装 JOIN + WHERE（~40 行）
- 3 个过滤条件（group/tag/type）独立判断、任意组合

### 其余问题 → docs/issue.md
- 记录 #3（无分页）、#5（全局 timer）、#6（重复动态 import）、#7（无乐观更新回滚）及其他低风险项

### 涉及文件
- `src-tauri/src/commands/items.rs` — 核心改动
- `docs/issue.md` — 新建